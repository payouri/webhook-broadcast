components:
  schemas:
    Attempt:
      additionalProperties: false
      properties:
        at:
          $ref: "#/components/schemas/DateTime"
        durationMs:
          $ref: "#/components/schemas/__schema105"
        error:
          $ref: "#/components/schemas/__schema106"
        id:
          $ref: "#/components/schemas/Id"
        n:
          $ref: "#/components/schemas/__schema103"
        statusCode:
          $ref: "#/components/schemas/__schema104"
      required:
        - id
        - n
        - statusCode
        - durationMs
        - error
        - at
      type: object
    AttemptList:
      additionalProperties: false
      properties:
        items:
          $ref: "#/components/schemas/__schema102"
        nextCursor:
          $ref: "#/components/schemas/__schema107"
      required:
        - items
        - nextCursor
      type: object
    BroadcastAccepted:
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/__schema95"
      required:
        - id
      type: object
    BroadcastDetail:
      additionalProperties: false
      properties:
        body:
          $ref: "#/components/schemas/__schema87"
        channelId:
          $ref: "#/components/schemas/Id"
        contentType:
          $ref: "#/components/schemas/__schema86"
        deliveries:
          $ref: "#/components/schemas/__schema88"
        id:
          $ref: "#/components/schemas/Id"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
      required:
        - id
        - channelId
        - receivedAt
        - contentType
        - body
        - deliveries
      type: object
    BroadcastEndpointDelivery:
      additionalProperties: false
      properties:
        attemptCount:
          $ref: "#/components/schemas/__schema84"
        deliveryId:
          $ref: "#/components/schemas/Id"
        lastDurationMs:
          $ref: "#/components/schemas/__schema82"
        lastError:
          $ref: "#/components/schemas/__schema83"
        lastStatusCode:
          $ref: "#/components/schemas/__schema81"
        status:
          $ref: "#/components/schemas/__schema80"
      required:
        - deliveryId
        - status
        - lastStatusCode
        - lastDurationMs
        - lastError
        - attemptCount
      type: object
    BroadcastList:
      additionalProperties: false
      properties:
        items:
          $ref: "#/components/schemas/__schema72"
        nextCursor:
          $ref: "#/components/schemas/__schema85"
      required:
        - items
        - nextCursor
      type: object
    BroadcastListItem:
      additionalProperties: false
      properties:
        bodyPreview:
          $ref: "#/components/schemas/__schema73"
        channelId:
          $ref: "#/components/schemas/Id"
        delivery:
          $ref: "#/components/schemas/__schema79"
        fanout:
          $ref: "#/components/schemas/FanoutSummary"
        id:
          $ref: "#/components/schemas/Id"
        receivedAt:
          $ref: "#/components/schemas/DateTime"
      required:
        - id
        - channelId
        - receivedAt
        - bodyPreview
        - fanout
      type: object
    Channel:
      additionalProperties: false
      properties:
        allowUnauthenticatedIngest:
          $ref: "#/components/schemas/__schema35"
        autoDisabledEndpointCount:
          $ref: "#/components/schemas/__schema41"
        createdAt:
          $ref: "#/components/schemas/DateTime"
        deletedAt:
          $ref: "#/components/schemas/__schema44"
        description:
          $ref: "#/components/schemas/__schema31"
        enabled:
          $ref: "#/components/schemas/__schema32"
        endpointCount:
          $ref: "#/components/schemas/__schema38"
        forwardHeaders:
          $ref: "#/components/schemas/__schema33"
        hasBroadcasts:
          $ref: "#/components/schemas/__schema39"
        id:
          $ref: "#/components/schemas/Id"
        ingestSuccessStatus:
          $ref: "#/components/schemas/__schema36"
        recentFailedDeliveryCount:
          $ref: "#/components/schemas/__schema40"
        slug:
          $ref: "#/components/schemas/__schema30"
        tokens:
          $ref: "#/components/schemas/__schema42"
        updatedAt:
          $ref: "#/components/schemas/DateTime"
      required:
        - id
        - slug
        - description
        - enabled
        - forwardHeaders
        - allowUnauthenticatedIngest
        - ingestSuccessStatus
        - endpointCount
        - hasBroadcasts
        - recentFailedDeliveryCount
        - autoDisabledEndpointCount
        - tokens
        - deletedAt
        - createdAt
        - updatedAt
      type: object
    ChannelCreate:
      properties:
        allowUnauthenticatedIngest:
          $ref: "#/components/schemas/__schema6"
        description:
          $ref: "#/components/schemas/__schema2"
        enabled:
          $ref: "#/components/schemas/__schema3"
        forwardHeaders:
          $ref: "#/components/schemas/__schema4"
        ingestSuccessStatus:
          $ref: "#/components/schemas/__schema7"
        slug:
          $ref: "#/components/schemas/__schema1"
      required:
        - slug
      type: object
    ChannelFailureRollup:
      additionalProperties: false
      properties:
        items:
          $ref: "#/components/schemas/__schema48"
      required:
        - items
      type: object
    ChannelList:
      additionalProperties: false
      properties:
        items:
          $ref: "#/components/schemas/__schema29"
        nextCursor:
          $ref: "#/components/schemas/__schema45"
      required:
        - items
        - nextCursor
      type: object
    ChannelTokenCreated:
      additionalProperties: false
      properties:
        createdAt:
          $ref: "#/components/schemas/DateTime"
        id:
          $ref: "#/components/schemas/Id"
        token:
          $ref: "#/components/schemas/__schema65"
      required:
        - id
        - token
        - createdAt
      type: object
    ChannelTokenSummary:
      additionalProperties: false
      properties:
        createdAt:
          $ref: "#/components/schemas/DateTime"
        id:
          $ref: "#/components/schemas/Id"
        prefix:
          $ref: "#/components/schemas/__schema43"
      required:
        - id
        - prefix
        - createdAt
      type: object
    ChannelUpdate:
      properties:
        allowUnauthenticatedIngest:
          $ref: "#/components/schemas/__schema13"
        description:
          $ref: "#/components/schemas/__schema10"
        enabled:
          $ref: "#/components/schemas/__schema11"
        forwardHeaders:
          $ref: "#/components/schemas/__schema12"
        ingestSuccessStatus:
          $ref: "#/components/schemas/__schema14"
        slug:
          $ref: "#/components/schemas/__schema9"
      type: object
    DateTime:
      description: ISO-8601; Zod wire type z.iso.datetime()
      format: date-time
      pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
      type: string
    Delivery:
      additionalProperties: false
      properties:
        attemptCount:
          $ref: "#/components/schemas/__schema98"
        broadcastId:
          $ref: "#/components/schemas/Id"
        channelId:
          $ref: "#/components/schemas/Id"
        endpointId:
          $ref: "#/components/schemas/Id"
        endpointName:
          $ref: "#/components/schemas/__schema96"
        endpointUrl:
          $ref: "#/components/schemas/__schema97"
        id:
          $ref: "#/components/schemas/Id"
        lastDurationMs:
          $ref: "#/components/schemas/__schema100"
        lastError:
          $ref: "#/components/schemas/__schema101"
        lastStatusCode:
          $ref: "#/components/schemas/__schema99"
        status:
          $ref: "#/components/schemas/__schema80"
        updatedAt:
          $ref: "#/components/schemas/DateTime"
      required:
        - id
        - broadcastId
        - channelId
        - endpointId
        - endpointName
        - endpointUrl
        - status
        - attemptCount
        - lastStatusCode
        - lastDurationMs
        - lastError
        - updatedAt
      type: object
    DeliveryItem:
      additionalProperties: false
      properties:
        attemptCount:
          $ref: "#/components/schemas/__schema91"
        endpointId:
          $ref: "#/components/schemas/Id"
        endpointName:
          $ref: "#/components/schemas/__schema89"
        endpointUrl:
          $ref: "#/components/schemas/__schema90"
        id:
          $ref: "#/components/schemas/Id"
        lastDurationMs:
          $ref: "#/components/schemas/__schema93"
        lastError:
          $ref: "#/components/schemas/__schema94"
        lastStatusCode:
          $ref: "#/components/schemas/__schema92"
        status:
          $ref: "#/components/schemas/__schema80"
        updatedAt:
          $ref: "#/components/schemas/DateTime"
      required:
        - id
        - endpointId
        - endpointName
        - endpointUrl
        - status
        - attemptCount
        - lastStatusCode
        - lastDurationMs
        - lastError
        - updatedAt
      type: object
    Endpoint:
      additionalProperties: false
      properties:
        autoDisabledAt:
          $ref: "#/components/schemas/__schema60"
        channelId:
          $ref: "#/components/schemas/Id"
        createdAt:
          $ref: "#/components/schemas/DateTime"
        enabled:
          $ref: "#/components/schemas/__schema59"
        headers:
          $ref: "#/components/schemas/__schema58"
        id:
          $ref: "#/components/schemas/Id"
        lastSuccessAt:
          $ref: "#/components/schemas/__schema63"
        name:
          $ref: "#/components/schemas/__schema55"
        p95Ms:
          $ref: "#/components/schemas/__schema62"
        successRate24h:
          $ref: "#/components/schemas/__schema61"
        timeoutMs:
          $ref: "#/components/schemas/__schema57"
        updatedAt:
          $ref: "#/components/schemas/DateTime"
        url:
          $ref: "#/components/schemas/__schema56"
      required:
        - id
        - channelId
        - name
        - url
        - timeoutMs
        - headers
        - enabled
        - createdAt
        - updatedAt
      type: object
    EndpointCreate:
      properties:
        enabled:
          $ref: "#/components/schemas/__schema22"
        headers:
          $ref: "#/components/schemas/__schema20"
        name:
          $ref: "#/components/schemas/__schema16"
        timeoutMs:
          $ref: "#/components/schemas/__schema18"
        url:
          $ref: "#/components/schemas/__schema17"
      required:
        - url
      type: object
    EndpointFailure:
      additionalProperties: false
      properties:
        autoDisabledAt:
          $ref: "#/components/schemas/__schema53"
        deadLettered:
          $ref: "#/components/schemas/__schema52"
        endpointId:
          $ref: "#/components/schemas/Id"
        endpointName:
          $ref: "#/components/schemas/__schema49"
        endpointUrl:
          $ref: "#/components/schemas/__schema50"
        failed:
          $ref: "#/components/schemas/__schema51"
        lastFailureAt:
          $ref: "#/components/schemas/DateTime"
      required:
        - endpointId
        - endpointName
        - endpointUrl
        - failed
        - deadLettered
        - autoDisabledAt
        - lastFailureAt
      type: object
    EndpointList:
      additionalProperties: false
      properties:
        items:
          $ref: "#/components/schemas/__schema54"
        nextCursor:
          $ref: "#/components/schemas/__schema64"
      required:
        - items
        - nextCursor
      type: object
    EndpointUpdate:
      properties:
        enabled:
          $ref: "#/components/schemas/__schema27"
        headers:
          $ref: "#/components/schemas/__schema26"
        name:
          $ref: "#/components/schemas/__schema23"
        timeoutMs:
          $ref: "#/components/schemas/__schema25"
        url:
          $ref: "#/components/schemas/__schema24"
      type: object
    ErrorEnvelope:
      additionalProperties: false
      properties:
        error:
          $ref: "#/components/schemas/__schema46"
      required:
        - error
      type: object
    FanoutSummary:
      additionalProperties: false
      properties:
        deadLettered:
          $ref: "#/components/schemas/__schema77"
        failed:
          $ref: "#/components/schemas/__schema76"
        pending:
          $ref: "#/components/schemas/__schema78"
        succeeded:
          $ref: "#/components/schemas/__schema75"
        total:
          $ref: "#/components/schemas/__schema74"
      required:
        - total
        - succeeded
        - failed
        - deadLettered
        - pending
      type: object
    Id:
      format: uuid
      pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
      type: string
    OperatorTokenCreate:
      properties:
        label:
          $ref: "#/components/schemas/__schema28"
      required:
        - label
      type: object
    OperatorTokenCreated:
      additionalProperties: false
      properties:
        createdAt:
          $ref: "#/components/schemas/DateTime"
        id:
          $ref: "#/components/schemas/Id"
        label:
          $ref: "#/components/schemas/__schema67"
        token:
          $ref: "#/components/schemas/__schema71"
      required:
        - id
        - token
        - label
        - createdAt
      type: object
    OperatorTokenList:
      additionalProperties: false
      properties:
        items:
          $ref: "#/components/schemas/__schema66"
        nextCursor:
          $ref: "#/components/schemas/__schema70"
      required:
        - items
        - nextCursor
      type: object
    OperatorTokenSummary:
      additionalProperties: false
      properties:
        createdAt:
          $ref: "#/components/schemas/DateTime"
        id:
          $ref: "#/components/schemas/Id"
        label:
          $ref: "#/components/schemas/__schema67"
        lastUsedAt:
          $ref: "#/components/schemas/__schema69"
        prefix:
          $ref: "#/components/schemas/__schema68"
      required:
        - id
        - label
        - prefix
        - createdAt
        - lastUsedAt
      type: object
    __schema0:
      type: string
    __schema1:
      maxLength: 200
      minLength: 1
      pattern: ^[a-z0-9]+(?:-[a-z0-9]+)*$
      type: string
    __schema10:
      anyOf:
        - type: string
        - type: "null"
    __schema100:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema101:
      anyOf:
        - type: string
        - type: "null"
    __schema102:
      items:
        $ref: "#/components/schemas/Attempt"
      type: array
    __schema103:
      maximum: 9007199254740991
      minimum: 1
      type: integer
    __schema104:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema105:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema106:
      anyOf:
        - type: string
        - type: "null"
    __schema107:
      anyOf:
        - type: string
        - type: "null"
    __schema11:
      type: boolean
    __schema12:
      $ref: "#/components/schemas/__schema5"
    __schema13:
      type: boolean
    __schema14:
      anyOf:
        - $ref: "#/components/schemas/__schema8"
        - type: "null"
    __schema15:
      type: string
    __schema16:
      minLength: 1
      type: string
    __schema17:
      format: uri
      type: string
    __schema18:
      $ref: "#/components/schemas/__schema19"
    __schema19:
      maximum: 3600000
      minimum: 1
      type: integer
    __schema2:
      type: string
    __schema20:
      $ref: "#/components/schemas/__schema21"
    __schema21:
      additionalProperties:
        type: string
      propertyNames:
        type: string
      type: object
    __schema22:
      default: true
      type: boolean
    __schema23:
      anyOf:
        - minLength: 1
          type: string
        - type: "null"
    __schema24:
      $ref: "#/components/schemas/__schema17"
    __schema25:
      anyOf:
        - $ref: "#/components/schemas/__schema19"
        - type: "null"
    __schema26:
      $ref: "#/components/schemas/__schema21"
    __schema27:
      type: boolean
    __schema28:
      description: Operator-chosen name identifying the holder, e.g. a CI job or laptop
      maxLength: 200
      minLength: 1
      type: string
    __schema29:
      items:
        $ref: "#/components/schemas/Channel"
      type: array
    __schema3:
      default: true
      type: boolean
    __schema30:
      minLength: 1
      type: string
    __schema31:
      anyOf:
        - type: string
        - type: "null"
    __schema32:
      type: boolean
    __schema33:
      description: Allow-listed inbound header names forwarded to every Endpoint on delivery. Empty by default.
      items:
        $ref: "#/components/schemas/__schema34"
      type: array
    __schema34:
      minLength: 1
      type: string
    __schema35:
      description: "Issue #38: when true, POST /ingest/:slug accepts this Channel's events without an ingest token — the slug is the only thing gating its fan-out. Off by default."
      type: boolean
    __schema36:
      anyOf:
        - $ref: "#/components/schemas/__schema37"
        - type: "null"
      description: "Issue #99: status returned by POST /ingest/:slug when this Channel accepts a Broadcast. null means inherit the service-wide INGEST_SUCCESS_STATUS (default 202); set it only for a producer whose success condition disagrees, so it does not retry an event that was already accepted. 204/205 are accepted but carry no body by HTTP rule, so the Broadcast id is not returned to the producer."
    __schema37:
      maximum: 299
      minimum: 200
      type: integer
    __schema38:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema39:
      description: "Issue #44: whether this Channel has any retained Broadcast — ADR 0002's retention window bounds this, so a Channel whose whole history has been swept reads as false again. false distinguishes 'no activity' from a healthy Channel: a Channel with zero recent failures because it has taken no traffic must not read the same as one that is actually fine."
      type: boolean
    __schema4:
      $ref: "#/components/schemas/__schema5"
      default: []
    __schema40:
      description: "Issue #44: count of this Channel's `failed` + `dead_lettered` Deliveries within the recent health window (packages/db's CHANNEL_RECENT_FAILURE_WINDOW_MS, currently 24h) — the single window constant every layer defers to, computed in one grouped query per list request rather than once per Channel."
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema41:
      description: "Issue #45: how many of this Channel's Endpoints are currently auto-disabled (ADR 0003) — `autoDisabledAt IS NOT NULL`, cleared the moment an operator re-enables one. An auto-disabled Endpoint is otherwise silent: the Channel keeps accepting Broadcasts while fan-out to that target quietly stops, so this is surfaced on the directory row rather than only on the Endpoints tab."
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema42:
      items:
        $ref: "#/components/schemas/ChannelTokenSummary"
      type: array
    __schema43:
      description: First 12 chars of the token — the `wbt_` wire prefix plus 8 of the secret — for recognition; never the full secret
      type: string
    __schema44:
      anyOf:
        - $ref: "#/components/schemas/DateTime"
        - type: "null"
    __schema45:
      anyOf:
        - type: string
        - type: "null"
    __schema46:
      additionalProperties: false
      properties:
        code:
          description: Stable machine code (e.g. validation_failed, not_found, unauthorized, conflict)
          type: string
        details:
          $ref: "#/components/schemas/__schema47"
          description: Present for validation_failed (Zod issues)
        message:
          type: string
      required:
        - code
        - message
      type: object
    __schema47:
      items:
        additionalProperties: false
        properties:
          message:
            type: string
          path:
            type: string
        required:
          - path
          - message
        type: object
      type: array
    __schema48:
      items:
        $ref: "#/components/schemas/EndpointFailure"
      type: array
    __schema49:
      anyOf:
        - type: string
        - type: "null"
    __schema5:
      items:
        minLength: 1
        type: string
      type: array
    __schema50:
      type: string
    __schema51:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema52:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema53:
      anyOf:
        - $ref: "#/components/schemas/DateTime"
        - type: "null"
      description: Non-null when this Endpoint is currently auto-disabled (ADR 0003) — ranks this entry above every non-auto-disabled Endpoint regardless of counts, since an auto-disabled Endpoint has stopped delivering and its counts have stopped growing.
    __schema54:
      items:
        $ref: "#/components/schemas/Endpoint"
      type: array
    __schema55:
      anyOf:
        - type: string
        - type: "null"
    __schema56:
      format: uri
      type: string
    __schema57:
      anyOf:
        - maximum: 3600000
          minimum: 1
          type: integer
        - type: "null"
    __schema58:
      additionalProperties:
        type: string
      propertyNames:
        type: string
      type: object
    __schema59:
      type: boolean
    __schema6:
      default: false
      type: boolean
    __schema60:
      anyOf:
        - $ref: "#/components/schemas/DateTime"
        - type: "null"
    __schema61:
      anyOf:
        - maximum: 1
          minimum: 0
          type: number
        - type: "null"
    __schema62:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema63:
      anyOf:
        - $ref: "#/components/schemas/DateTime"
        - type: "null"
    __schema64:
      anyOf:
        - type: string
        - type: "null"
    __schema65:
      description: Plaintext ingest token — shown once
      type: string
    __schema66:
      items:
        $ref: "#/components/schemas/OperatorTokenSummary"
      type: array
    __schema67:
      description: Operator-chosen name identifying the holder, e.g. a CI job or laptop
      maxLength: 200
      minLength: 1
      type: string
    __schema68:
      description: First 12 chars of the token — the `wbop_` wire prefix plus 7 of the secret — for recognition; never the full secret
      type: string
    __schema69:
      anyOf:
        - $ref: "#/components/schemas/DateTime"
        - type: "null"
    __schema7:
      anyOf:
        - $ref: "#/components/schemas/__schema8"
        - type: "null"
      default: null
    __schema70:
      anyOf:
        - type: string
        - type: "null"
    __schema71:
      description: Plaintext operator token — shown once
      type: string
    __schema72:
      items:
        $ref: "#/components/schemas/BroadcastListItem"
      type: array
    __schema73:
      type: string
    __schema74:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema75:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema76:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema77:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema78:
      description: pending + in_progress for list summaries
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema79:
      $ref: "#/components/schemas/BroadcastEndpointDelivery"
    __schema8:
      maximum: 299
      minimum: 200
      type: integer
    __schema80:
      enum:
        - pending
        - in_progress
        - succeeded
        - failed
        - dead_lettered
      type: string
    __schema81:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema82:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema83:
      anyOf:
        - type: string
        - type: "null"
    __schema84:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema85:
      anyOf:
        - type: string
        - type: "null"
    __schema86:
      type: string
    __schema87:
      description: Raw inbound body (UTF-8 text for MVP sketch)
      type: string
    __schema88:
      items:
        $ref: "#/components/schemas/DeliveryItem"
      type: array
    __schema89:
      anyOf:
        - type: string
        - type: "null"
    __schema9:
      $ref: "#/components/schemas/__schema1"
    __schema90:
      type: string
    __schema91:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema92:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema93:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
    __schema94:
      anyOf:
        - type: string
        - type: "null"
    __schema95:
      $ref: "#/components/schemas/Id"
      description: New Broadcast id from replay
    __schema96:
      anyOf:
        - type: string
        - type: "null"
    __schema97:
      type: string
    __schema98:
      maximum: 9007199254740991
      minimum: 0
      type: integer
    __schema99:
      anyOf:
        - maximum: 9007199254740991
          minimum: -9007199254740991
          type: integer
        - type: "null"
  securitySchemes:
    OperatorBearer:
      bearerFormat: API key
      description: "The bootstrap OPERATOR_API_KEY, or any minted operator_token (issue #41) — both are accepted as Bearer credentials with equal privilege. Only the bootstrap key may also be exchanged for the dashboard's HttpOnly session cookie via POST /auth/login; the cookie itself is accepted here as an alternate to Bearer, not a separate OAuth/user session."
      scheme: bearer
      type: http
    OperatorCookie:
      description: Optional alternate to OperatorBearer for the dashboard.
      in: cookie
      name: wb_operator
      type: apiKey
externalDocs:
  description: Ingest request/response shape and auth, documented in full
  url: https://github.com/payouri/webhook-broadcast/blob/trunk/CONTEXT.md
info:
  description: |-
    Operator-facing admin contract emitted from Zod schemas in packages/contract for Channel/Endpoint/Broadcast/Delivery administration.

    `POST /ingest/{slug}` is deliberately NOT a path in this document (ADR 0005) — it authenticates with the Channel's own ingest token rather than an operator credential, accepts an arbitrary request body with no useful schema, and a generated `ingest()` client method typed `body: unknown` would be noise. It still exists and can be called: `POST /ingest/{slug}` with `Authorization: Bearer <channel ingest token>` (omit the header entirely when the Channel has opted into unauthenticated ingest), any request body relayed verbatim to every enabled Endpoint on that Channel, answering a 2xx on acceptance whose exact status defaults to `202` but is configurable service-wide via `INGEST_SUCCESS_STATUS` and per-Channel via `ingestSuccessStatus`. The accepted response carries the id of the new Broadcast, except under a configured `204`/`205`, which carry no body by HTTP rule. The linked documentation covers ingest in full.
  title: webhook-broadcast Admin API
  version: 0.1.0
openapi: 3.1.0
paths:
  /channels:
    get:
      operationId: listChannels
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            default: 50
            maximum: 100
            minimum: 1
            type: integer
        - description: Exact match on channel.slug (UNIQUE) — at most one row
          in: query
          name: slug
          schema:
            $ref: "#/components/schemas/__schema0"
            description: Exact match on channel.slug (UNIQUE) — at most one row
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelList"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: List channels
      tags:
        - channels
    post:
      operationId: createChannel
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChannelCreate"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Create channel
      tags:
        - channels
  /channels/{channelId}:
    delete:
      operationId: deleteChannel
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "204":
          description: Soft-deleted
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Soft-delete channel (sets deletedAt)
      tags:
        - channels
    get:
      operationId: getChannel
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - channels
    patch:
      operationId: updateChannel
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChannelUpdate"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - channels
  /channels/{channelId}/broadcasts:
    get:
      operationId: listChannelBroadcasts
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            default: 50
            maximum: 100
            minimum: 1
            type: integer
        - in: query
          name: endpointId
          schema:
            $ref: "#/components/schemas/Id"
        - in: query
          name: status
          schema:
            enum:
              - failed
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BroadcastList"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Channel activity (newest first)
      tags:
        - broadcasts
  /channels/{channelId}/broadcasts/{broadcastId}:
    get:
      operationId: getBroadcastDetail
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: path
          name: broadcastId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BroadcastDetail"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Broadcast detail with fanned-out Deliveries
      tags:
        - broadcasts
  /channels/{channelId}/broadcasts/{broadcastId}/replay:
    post:
      operationId: replayBroadcast
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: path
          name: broadcastId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BroadcastAccepted"
          description: Accepted
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Accept a new Broadcast from stored payload (202 semantics)
      tags:
        - broadcasts
  /channels/{channelId}/endpoints:
    get:
      operationId: listEndpoints
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            default: 50
            maximum: 100
            minimum: 1
            type: integer
        - description: Exact match on endpoint.url within this channel (UNIQUE) — at most one row
          in: query
          name: url
          schema:
            $ref: "#/components/schemas/__schema15"
            description: Exact match on endpoint.url within this channel (UNIQUE) — at most one row
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EndpointList"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - endpoints
    post:
      operationId: createEndpoint
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndpointCreate"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endpoint"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - endpoints
  /channels/{channelId}/endpoints/{endpointId}:
    delete:
      operationId: deleteEndpoint
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: path
          name: endpointId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "204":
          description: Deleted
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Hard-delete endpoint, freeing its (channelId, url)
      tags:
        - endpoints
    get:
      operationId: getEndpoint
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: path
          name: endpointId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endpoint"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - endpoints
    patch:
      operationId: updateEndpoint
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: path
          name: endpointId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndpointUpdate"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endpoint"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - endpoints
  /channels/{channelId}/failures:
    get:
      operationId: listChannelFailures
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelFailureRollup"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Ranked failure roll-up by Endpoint (24h window)
      tags:
        - channels
  /channels/{channelId}/tokens:
    post:
      operationId: createChannelToken
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelTokenCreated"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Create ingest token; plaintext returned once
      tags:
        - tokens
  /channels/{channelId}/tokens/{tokenId}:
    delete:
      operationId: revokeChannelToken
      parameters:
        - in: path
          name: channelId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
        - in: path
          name: tokenId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "204":
          description: Revoked
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - tokens
  /deliveries/{deliveryId}:
    get:
      operationId: getDelivery
      parameters:
        - in: path
          name: deliveryId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - deliveries
  /deliveries/{deliveryId}/attempts:
    get:
      operationId: listDeliveryAttempts
      parameters:
        - in: path
          name: deliveryId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttemptList"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      tags:
        - deliveries
  /deliveries/{deliveryId}/retry:
    post:
      operationId: retryDelivery
      parameters:
        - in: path
          name: deliveryId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"
          description: Delivery re-queued as pending
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Re-enqueue a dead_lettered Delivery (409 if not dead_lettered)
      tags:
        - deliveries
  /operator-tokens:
    get:
      operationId: listOperatorTokens
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorTokenList"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: "List operator token summaries (issue #41)"
      tags:
        - operator-tokens
    post:
      operationId: createOperatorToken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OperatorTokenCreate"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorTokenCreated"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Mint an operator token; plaintext returned once
      tags:
        - operator-tokens
  /operator-tokens/{tokenId}:
    delete:
      operationId: revokeOperatorToken
      parameters:
        - in: path
          name: tokenId
          required: true
          schema:
            $ref: "#/components/schemas/Id"
      responses:
        "204":
          description: Revoked
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - OperatorBearer: []
        - OperatorCookie: []
      summary: Revoke an operator token immediately, no redeploy required
      tags:
        - operator-tokens
security:
  - OperatorBearer: []
  - OperatorCookie: []
servers:
  - url: /
tags:
  - name: channels
  - name: endpoints
  - name: tokens
  - name: operator-tokens
  - name: broadcasts
  - name: deliveries
