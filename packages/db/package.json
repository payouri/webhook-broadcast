{
  "name": "@webhook-broadcast/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": ">=0.45.0 <1.0.0",
    "drizzle-zod": "^0.8.3",
    "pg": "^8.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.0.0",
    "@webhook-broadcast/contract": "workspace:*",
    "drizzle-kit": ">=0.30.0 <1.0.0",
    "typescript": "^6.0.0"
  }
}
